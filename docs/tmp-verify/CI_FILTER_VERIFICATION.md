# CI filter verification

Throwaway file to confirm the `dorny/paths-filter` fix in PR #117
(`predicate-quantifier: every`) actually skips `test`/`frontend-test` on a
docs/markdown-only PR. Safe to delete after the check is observed.
