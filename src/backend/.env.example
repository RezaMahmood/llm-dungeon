# Do not commit this file with actual values; use Key Vault / Function App settings in production
AZURE_TENANT_ID=
AZURE_APP_ID=
COSMOS_ENDPOINT=
AZURE_AI_FOUNDRY_ENDPOINT=
AZURE_AI_FOUNDRY_DEPLOYMENT_NAME=
LLM_INPUT_TOKEN_PRICE_USD=
LLM_OUTPUT_TOKEN_PRICE_USD=
# Optional. Overrides every call's own reasoning effort (minimal|low|medium|high).
# Leave blank to keep the per-call defaults; "off" omits the parameter entirely.
LLM_REASONING_EFFORT=
SEED_ADMIN_EMAIL=
FRONTEND_URL=
