// Re-exported from CapabilitiesContext.jsx (2026-08-31: moved to a single
// shared fetch mounted once at the app root — see that file's header comment
// for why). Kept at this path so every existing import site and test mock
// keeps working unchanged.
export { useCapabilities, CapabilitiesProvider, default } from "../context/CapabilitiesContext.jsx";
