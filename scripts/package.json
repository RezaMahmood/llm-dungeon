{
  "name": "llmdungeon-ci-scripts",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Automated tests for the CI/CD pipeline itself (specs/023-cicd-pipeline-optimization). Not part of the application. Package-specific checks live in scripts/workflow-checks and scripts/release-fixtures (split per issue #192) so each CI job installs only the dependencies it actually needs.",
  "scripts": {
    "test:pr-title": "node test-pr-title.js"
  }
}
